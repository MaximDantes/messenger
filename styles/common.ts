import {StyleSheet} from 'react-native'

export const screenStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'flex-start',
    }
})

export const headerStyles = StyleSheet.create({
    container: {
        // backgroundColor: '#570f67',
        // color: '#fff',
    }
})